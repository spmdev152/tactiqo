[project]
name = "tactiqo-backend"
version = "0.1.0"
description = "Tactiqo football intelligence backend: Django, Django Ninja, and Celery."
requires-python = ">=3.13"
dependencies = [
  "celery[redis]>=5.4",
  "django>=5.2,<5.3",
  "django-ninja>=1.3",
  "httpx>=0.28",
  "loguru>=0.7",
  "psycopg[binary]>=3.2",
  "python-dotenv>=1.0",
  "redis>=5.2",
]

[dependency-groups]
lint = ["ruff>=0.9", "taplo>=0.9.3"]
typecheck = ["django-stubs>=5.1", "pyright>=1.1.393"]
test = ["pytest>=8.3", "pytest-django>=4.9"]
dev = [
  { include-group = "lint" },
  { include-group = "typecheck" },
  { include-group = "test" },
]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py313"
extend-exclude = ["**/migrations/**"]

[tool.ruff.lint]
select = [
  "ANN",
  "ARG",
  "B",
  "D",
  "DJ",
  "E",
  "F",
  "I",
  "N",
  "PL",
  "PTH",
  "RUF",
  "S",
  "SIM",
  "TID",
  "UP",
  "W",
]
ignore = ["D100", "D104", "D200", "D202"]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.per-file-ignores]
"config/settings/{local,preproduction,production,test}.py" = ["F403", "F405"]
"tests/**" = ["D205", "D400", "S101"]

[tool.ruff.format]
docstring-code-format = true

[tool.pyright]
pythonVersion = "3.13"
typeCheckingMode = "standard"
include = ["apps", "config", "integrations", "tests", "manage.py"]
exclude = ["**/migrations/**", "**/__pycache__", ".venv"]
reportMissingTypeStubs = false

# django-stubs declares `Model.objects` as `ClassVar[Manager[Self]]`. A mutable
# class variable is invariant, so a model cannot declare its own manager type at
# all: the declaration below is correct and the stub simply cannot express it.
[[tool.pyright.executionEnvironments]]
root = "apps/accounts/models.py"
extraPaths = ["."]
reportIncompatibleVariableOverride = "none"

# django-stubs types the nested `Meta` of the bundled auth forms against the
# concrete `django.contrib.auth.models.User` and types
# `UserChangeForm.Meta.fields` as a plain string, so no `Meta` that binds a
# swapped user model to a tuple of fields can ever be assignable to it.
[[tool.pyright.executionEnvironments]]
root = "apps/accounts/admin.py"
extraPaths = ["."]
reportIncompatibleVariableOverride = "none"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-ra --strict-markers --strict-config --ds=config.settings.test"
